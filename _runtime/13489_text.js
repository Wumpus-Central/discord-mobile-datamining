// _runtime/13489_text.js

export default (arg0) => {
  const tmp = require("metro/13490__.js")(arg0, "string");
  let text = tmp;
  if (!require("metro/13504__.js")(tmp)) {
    text = `${tmp}`;
  }
  return text;
};