// === Module 4336: toInteger ===

// Module 4336 (toInteger)

export default function toInteger(arg0) {
  const tmp = require(4337) /* toFinite */(arg0);
  const result = tmp % 1;
  let num = 0;
  if (tmp == tmp) {
    let diff = tmp;
    if (result) {
      diff = tmp - result;
    }
    num = diff;
  }
  return num;
};