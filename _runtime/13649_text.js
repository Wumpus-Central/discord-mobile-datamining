// _runtime/13649_text.js
import { 13650__ } from "metro/13650__.js";
import { 13664__ } from "metro/13664__.js";

export default (arg0) => {
  const tmp = 13650__(arg0, "string");
  let text = tmp;
  if (!13664__(tmp)) {
    text = `${tmp}`;
  }
  return text;
};