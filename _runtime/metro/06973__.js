// _runtime/metro/06973__.js
import _mod6974 from "06974__.js";

export default function toPropertyKey(arg0) {
  const tmp = _mod6974(arg0, "string");
  let text = tmp;
  if ("symbol" != obj.default(tmp)) {
    text = `${tmp}`;
  }
  return text;
}
