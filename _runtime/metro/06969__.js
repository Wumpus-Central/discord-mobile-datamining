// _runtime/metro/06969__.js
import _mod6970 from "06970__.js";

export default function toPropertyKey(arg0) {
  const tmp = _mod6970(arg0, "string");
  let text = tmp;
  if ("symbol" != obj.default(tmp)) {
    text = `${tmp}`;
  }
  return text;
}
