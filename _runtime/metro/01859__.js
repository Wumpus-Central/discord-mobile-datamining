// _runtime/metro/01859__.js
import TEST_ID_KEYBOARD_TOOLBAR from "../01854_TEST_ID_KEYBOARD_TOOLBAR.js";
import noop from "00019__.js";

require = fn;
get_ActivityIndicator = fn(17);
({ StyleSheet, View: c2 } = get_ActivityIndicator);
const jsx = fn(21).jsx;
const styles = StyleSheet.create({ flex: { flex: 1 } });

export default function _default(children) {
  return (
    <React2 style={styles.flex} testID={TEST_ID_KEYBOARD_TOOLBAR.TEST_ID_KEYBOARD_TOOLBAR_CONTENT}>
      {arg0.children}
    </React2>
  );
}
