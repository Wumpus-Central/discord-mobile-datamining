// === Module 7739: getError ===

// Module 7739 (getError)
import set from "set" /* 2 */;

const result = set.fileFinishedImporting("modules/auth/native/getError.tsx");

export default function getError(arg0, arg1) {
  let tmp2 = null;
  if (null != arg1[arg0]) {
    const _Array = Array;
    let first = tmp;
    if (Array.isArray(tmp)) {
      first = tmp[0];
    }
    tmp2 = first;
  }
  return tmp2;
};