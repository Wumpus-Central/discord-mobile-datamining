// === Module 10348: useWishlistApplicationIds ===

// Module 10348 (useWishlistApplicationIds)
import noop from "noop" /* 19 */;
import { COLLECTIBLES_APPLICATION_ID as closure_1 } from "ME" /* 676 */;

const result = require("obj132").fileFinishedImporting("modules/wishlists/hooks/useWishlistApplicationIds.native.tsx");

export const useWishlistApplicationIds = function useWishlistApplicationIds(userId) {
  return React.useMemo(() => {
    const items = [closure_1];
    return items;
  }, []);
};